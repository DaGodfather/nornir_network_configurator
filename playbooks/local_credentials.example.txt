# Local Credentials Configuration Example
# This file defines local user accounts and password policies that should be configured on network devices
# Format: One configuration command per line
# Lines starting with # are comments and will be ignored

# Local user accounts
# Note: Passwords should be pre-hashed or configured securely
# Format: username <name> privilege <level> secret <password>
username admin privilege 15 secret MySecureP@ss123
username netops privilege 15 secret NetOpsP@ss456
username readonly privilege 1 secret ReadOnlyP@ss789

# Password policy settings
service password-encryption
security passwords min-length 12

# Optional: Enable secret (if different from line password)
# enable secret EnableP@ss999

# Optional: AAA settings for local authentication
# aaa new-model
# aaa authentication login default local
# aaa authorization exec default local
